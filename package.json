{
  "name": "gstack-workspace",
  "private": true,
  "scripts": {
    "test": "bun --cwd gstack test",
    "build": "bun --cwd gstack run build",
    "dev": "bun --cwd gstack run dev",
    "install": "bun --cwd gstack install"
  }
}
